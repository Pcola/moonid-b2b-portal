# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (NEVER commit secrets) — pokrýva .env, .env.local, .env.production, .env.staging...
.env*
!.env.example
!.env.test.example

# Playwright — session storageState (auth tokeny!) + reporty, NIKDY necommituj
/tests/e2e/.auth/
/playwright-report/
/test-results/

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# editors
.idea/
.vscode/

# prisma local
/prisma/*.db

# claude code local settings
.claude/settings.local.json

# lokálne dátové exporty z Pohody (biznis dáta — nepatria do gitu)
/data/

# python build artefakty (adhoc enrich/EAN skripty)
__pycache__/
*.pyc

# .NET publish výstup (agent) — binárky nepatria do gitu
/agent/publish-fd/

# Repozitár je VEREJNÝ (kvôli branch protection na free účte). Nasledujúce artefakty
# do neho nepatria — zálohy sú mimo repo v ../moonid_private/.

# dizajnové prototypy: obsahujú názvy reálnych firiem s vymyslenými tržbami a cenovými
# hladinami; appka ich nepoužíva
/prototypes/

# vyexportovaný katalóg dodávateľa (1955 položiek + hotlink na jeho CDN); appka ho nepoužíva
/content/products.json

# auditné dokumenty = mapa slabých miest, časť nálezov je stále otvorená
/docs/SECURITY_AUDIT*.md
/docs/ENTERPRISE_AUDIT*.md
/docs/ENTERPRISE_DEEP_AUDIT*.md
/docs/ENTERPRISE_GAPS*.md
/docs/GO_LIVE_AUDIT*.md
/docs/SECURITY_TRIAGE.md
/docs/PRODUCTION_DB_ROLE_RUNBOOK.md
