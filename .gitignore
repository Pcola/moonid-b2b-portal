# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (NEVER commit secrets) — pokrýva .env, .env.local, .env.production, .env.staging...
.env*
!.env.example
!.env.test.example

# Playwright — session storageState (auth tokeny!) + reporty, NIKDY necommituj
/tests/e2e/.auth/
/playwright-report/
/test-results/

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# editors
.idea/
.vscode/

# prisma local
/prisma/*.db

# claude code local settings
.claude/settings.local.json

# lokálne dátové exporty z Pohody (biznis dáta — nepatria do gitu)
/data/

# python build artefakty (adhoc enrich/EAN skripty)
__pycache__/
*.pyc

# .NET publish výstup (agent) — binárky nepatria do gitu
/agent/publish-fd/
